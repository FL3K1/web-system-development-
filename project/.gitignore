# Dependencias (se instalan con npm install, no se suben)
node_modules
/server/node_modules
/client/node_modules

# Archivos de entorno (SECRETOS - NUNCA SUBIR)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Builds de producción (se generan automáticamente)
dist
build
/client/dist

# Logs y archivos del sistema
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
Thumbs.db

# Base de datos local (si usaras SQLite)
*.sqlite
*.sqlite3
*.db